overwrite: true
schema: "../api/schema.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    config:
      useIndexSignature: true # apollo-server compatibility
    plugins:
      - "typescript"
      - "typescript-resolvers"
